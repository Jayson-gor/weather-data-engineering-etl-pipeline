pytest
pytest-cov
requests
requests-mock
testcontainers[postgres]>=4.7.2
psycopg2-binary
# Note: Airflow is NOT installed in the QA venv to avoid Py3.12 conflicts.
# DAG import test will skip if Airflow isn't available locally.
# Optional: if you have Python 3.11, you can install Airflow for import tests.
# apache-airflow==3.0.5

# Optional dbt sanity
# dbt-postgres==1.9.0
